{
  "name": "vessify",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run install:backend && npm run install:frontend",
    "install:backend": "cd backend && npm install --include=dev",
    "install:frontend": "cd frontend && npm install --include=dev",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
     "build": "npm install --prefix backend && npm install --prefix frontend --include=dev && npm run build --prefix frontend", 
    "migrate:deploy": "cd backend && npx prisma migrate deploy",
    "start:backend": "cd backend && npm run start",
    "start:frontend": "cd frontend && npm run start",
    "start": "npx concurrently -k -s first \"PORT=8787 npm run start:backend\" \"npm run start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
